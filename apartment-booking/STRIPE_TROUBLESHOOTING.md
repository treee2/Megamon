# 🔧 Решение проблем со Stripe

## Проблема: Предупреждение "You may test your Stripe.js integration over HTTP"

### Что это значит?
Это **НЕ ошибка**, а предупреждение. Stripe напоминает, что в продакшене нужно использовать HTTPS.

### Решение:
✅ **Для локальной разработки** - игнорируйте это предупреждение  
✅ **Для продакшена** - используйте HTTPS (Railway, Vercel, Heroku автоматически предоставляют HTTPS)

---

## Проблема: "Neither apiKey nor config.authenticator provided"

### Причина:
Сервер не находит `STRIPE_SECRET_KEY` в переменных окружения.

### Решение:
1. Проверьте, что файл `server/.env` существует
2. Убедитесь, что в нём есть строка:
   ```env
   STRIPE_SECRET_KEY=sk_test_ваш_ключ
   ```
3. Запустите проверку окружения:
   ```bash
   cd server
   npm run check-env
   ```

---

## Проблема: Не загружаются Stripe компоненты

### Причина:
Пакеты `@stripe/stripe-js` и `@stripe/react-stripe-js` не установлены.

### Решение:
```bash
cd apartment-booking
npm install @stripe/stripe-js @stripe/react-stripe-js
```

---

## Проблема: Оплата не проходит в компоненте StripeCheckoutForm

### Проверьте:
1. ✅ `clientSecret` успешно создан (смотрите в консоли браузера)
2. ✅ Используете тестовую карту `4242 4242 4242 4242`
3. ✅ Все поля формы заполнены
4. ✅ В консоли браузера нет ошибок

### Типичные ошибки и решения:

#### Ошибка: "Your card was declined"
- Используйте правильную тестовую карту: `4242 4242 4242 4242`

#### Ошибка: "Invalid request: amount must be at least"
- Проверьте, что `amount` в createPaymentIntent умножается на 100 (Stripe работает в копейках)

#### Ошибка: "No such PaymentIntent"
- Payment Intent не был создан. Проверьте логи сервера

---

## Полезные команды для диагностики

### Проверить переменные окружения сервера:
```bash
cd server
npm run check-env
```

### Посмотреть логи создания Payment Intent:
Откройте консоль браузера (F12) → вкладка Console  
Ищите сообщения с префиксами: 🔄 📤 ✅ ❌

### Проверить состояние Stripe на сервере:
Сервер выводит `✅ Stripe инициализирован успешно` при запуске

---

## Тестовые данные карты

Для тестирования используйте:
- **Номер:** 4242 4242 4242 4242
- **Срок:** 12/34 (любая будущая дата)
- **CVC:** 123 (любые 3 цифры)
- **ZIP:** 12345 (любой)

Полный список тестовых карт: см. `STRIPE_TEST_CARDS.md`

---

## Где получить API ключи?

1. Зарегистрируйтесь на https://stripe.com
2. Перейдите в Dashboard: https://dashboard.stripe.com/apikeys
3. Скопируйте:
   - **Publishable key** (начинается с `pk_test_`) → `.env` в корне
   - **Secret key** (начинается с `sk_test_`) → `server/.env`

⚠️ **ВАЖНО:** Никогда не коммитьте файлы `.env` в Git!

---

## Дополнительная помощь

- 📚 Документация Stripe: https://docs.stripe.com
- 💬 Поддержка: https://support.stripe.com
- 🧪 Тестовый Dashboard: https://dashboard.stripe.com/test/payments
